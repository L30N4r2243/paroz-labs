version: "3.9"
services:
  backend:
    env_file:
      - .env
    build: .
    container_name: flask-backend
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
